<%= form_with(model: demande) do |form| %>
  <% if demande.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(demande.errors.count, "error") %> prohibited this demande from being saved:</h2>

      <ul>
        <% demande.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name, onkeydown:"name1.innerHTML=this.value;" %>
  </div>

  <div><span style="color:white;" id="myicon"></span>
      <span id="name1">
  </span>
  </div>


  <div>
    <%= form.label :image, style: "display: block" %>
    <%= form.text_field :image,onkeydown:"myicon.innerHTML=this.value;" %>
  </div>

  <div>
    <%= form.label :icon, style: "display: block" %>
    de quelle couleur?
    <%= form.color_field :icon,onchange:"myicon.style.background=this.value;" %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
