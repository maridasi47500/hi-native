<%= form_with(model: learning_level) do |form| %>
  <% if learning_level.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(learning_level.errors.count, "error") %> prohibited this learning_level from being saved:</h2>

      <ul>
        <% learning_level.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
